[package]
name = "sabi-server"
version = "0.1.0"
edition = "2024"

[dependencies]
sabi-core = { path = "../sabi-core" }

tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"