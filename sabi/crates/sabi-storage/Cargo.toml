[package]
name = "sabi-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
sabi-core = { path = "../sabi-core" }
crc32fast = "1.4"
uuid = { version = "1", features = ["serde", "v7"] }

[dev-dependencies]
tempfile = "3"