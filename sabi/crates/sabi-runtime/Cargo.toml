[package]
name = "sabi-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
sabi-core = { path = "../sabi-core" }
sabi-storage = { path = "../sabi-storage" }
sabi-sql = { path = "../sabi-sql" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0.17"