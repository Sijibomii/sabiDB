[workspace]
members = [
    "crates/sabi-core", "crates/sabi-runtime", "crates/sabi-server", "crates/sabi-sql", "crates/sabi-storage",
]

resolver = "2"

[workspace.metadata]
# Workspace-level metadata

# Root package for integration tests
[package]
name = "sabi-workspace"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = []
integration-tests = []

[dev-dependencies]
# For integration tests
